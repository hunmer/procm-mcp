services:
  redis-test:
    image: redis:8-alpine
    container_name: procm-redis-test
    ports:
      - "16379:6379"
  nginx-test:
    image: nginx:alpine
    container_name: procm-nginx-test
    ports:
      - "8080:80"
    volumes:
      - ./nginx-test.conf:/etc/nginx/nginx.conf:ro
